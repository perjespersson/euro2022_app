<%= form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :home_team_id, style: "display: block" %>
    <%= form.text_field :home_team_id %>
  </div>

  <div>
    <%= form.label :away_team_id, style: "display: block" %>
    <%= form.text_field :away_team_id %>
  </div>

  <div>
    <%= form.label :home_team_score, style: "display: block" %>
    <%= form.number_field :home_team_score %>
  </div>

  <div>
    <%= form.label :away_team_score, style: "display: block" %>
    <%= form.number_field :away_team_score %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
